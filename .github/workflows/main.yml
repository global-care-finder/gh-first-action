on:
  workflow_run:
    workflows: ["Build"]
    types: [requested]
    branches: 
      - 'releases/**'
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing workflow"
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build workflow"