name: CI
on: push
jobs: 
  prod-check:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      First_Name: Ferat
      Last_Name: Imrak
    steps:
      - run: echo "My name is $First_Name , last name is $Last_Name"