name: CI
on: push
jobs:
  prod-check:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps: 
      - name: Run build script
        run: ./.github/scripts/build.sh
        shell: bash 